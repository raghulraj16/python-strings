string="mississippi"
def most_frequent(string);
    d = dict()
    for key in dtring:
        if key not in d:
            d[key]=1
        else:
            d[key]+=1
    return d
print (most_frequent("mississippi"))            
